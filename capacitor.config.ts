import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.howardyang2009.20230214',
  appName: 'myApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
